#include "main.h"
#include <stdio.h>

/**
 * _isdigit - multiplies two integers
 * @a: the 1st caracter to multiplie
 * @b: the 2nd caracter to multiplie
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return(a * b);
}
