#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: the 1st character to multiplie
 * @b: the 2nd character to multiplie
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
