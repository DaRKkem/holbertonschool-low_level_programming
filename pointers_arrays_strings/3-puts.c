#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string, followed by a new line, to stdout
 * @str: the string
 *
 * Return: Always nothing.
 */
void _puts(char *str)
{
	char *s = str;
	printf("%s\n", s);
}
